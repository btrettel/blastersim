UserWarn
{ # case sensitive
    #\href # I wanted to replace all `\href`s with citations, but that's not necessarily the best approach.
    spring\_pe # deprecated variable name, replace with e\_s
    e\_spring # deprecated variable name, replace with e\_s
    m\_p\_ke # deprecated variable name, replace with e\_p
    ** # indicates a Fortran format problem
}
[ # case insensitive
    piston # write plunger
    plunger/projectile # write projectile/plunger
    non-linear # write nonlinear
    "spring constant" # write spring stiffness
    dart # write projectile unless specifically referring to foam dart blasters
    "barrel diameter" # write barrel inner diameter
    "projectile diameter" # write projectile outer diameter
]

UserWarnRegex
{
}
